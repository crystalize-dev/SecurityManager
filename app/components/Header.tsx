import React from 'react';

const Header = () => {
    return (
        <nav className="flex h-16 max-h-16 w-full items-center border-b border-solid border-black/10 bg-white">
            Header
        </nav>
    );
};

export default Header;
